{
    "name": "action-trigger",
    "version": "1.0.0",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc && ncc build ./build/index.js -o ./dist",
        "clean-build": "rimraf ./build",
        "clean-dist": "rimraf ./dist",
        "clean": "yarn clean-build && yarn clean-dist"
    },
    "repository": "git@github.com:flow-os/action-trigger.git",
    "author": "Laurentiu Ciobanu <laurentiu.ciobanu@flowos.com>",
    "license": "MIT",
    "dependencies": {
        "@actions/core": "^1.6.0",
        "@actions/github": "^5.0.0",
        "node-fetch": "^3.3.2"
    },
    "devDependencies": {
        "@types/node": "^17.0.16",
        "@vercel/ncc": "^0.33.1",
        "prettier": "^2.5.1",
        "rimraf": "^3.0.2",
        "typescript": "^4.5.5"
    }
}
